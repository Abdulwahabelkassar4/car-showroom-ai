<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Confirm Purchase</title>
  <link rel="stylesheet" href="/css/ridex_dark_theme.css">
</head>
<body>
  <%- include('partials/navbar') %>

<div class="confirm-wrapper">
    <h2 style="color: gold;">Confirm Your Purchase</h2>

    <img src="/<%= car.image %>" alt="<%= car.make %>" style="width: 100%; margin-bottom: 20px; border-radius: 8px;">

    <p><strong>Make:</strong> <%= car.make %></p>
    <p><strong>Model:</strong> <%= car.model %></p>
    <p><strong>Year:</strong> <%= car.year %></p>
    <p><strong>Price:</strong> $<%= car.price %></p>

    <hr style="margin: 25px 0; border-color: gold;">

    <p><strong>Your Name:</strong> <%= user.name %></p>
    <p><strong>Your Email:</strong> <%= user.email %></p>

    <form action="/buy/<%= car.id %>" method="POST" style="margin-top: 20px;">
      
      <div style="margin: 20px 0;">
        <label style="color: #ccc;">
          <input type="checkbox" name="confirm" required />
          I confirm that I want to purchase this vehicle.
        </label>
      </div>

      <button type="submit" class="btn" style="background: gold; color: white; font-weight: bold; width: 100%;">
        Confirm Purchase
      </button>
    </form>
  </div>
</body>
</html>
