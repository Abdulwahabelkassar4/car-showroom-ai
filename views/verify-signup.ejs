<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Verify Your Email</title>
  <link rel="stylesheet" href="/css/ridex_dark_theme.css" />
</head>
<body>
  <%- include('partials/navbar') %>

<div class="verify-signup-wrapper">
    <h2 style="text-align: center; color: gold;">Verify Your Email</h2>
    <p style="text-align: center;">We sent a verification code to <strong><%= email %></strong></p>

    <% if (error) { %>
      <p style="color: red; text-align: center;"><%= error %></p>
    <% } %>

    <form action="/verify-signup" method="POST" style="margin-top: 30px;">
      <input type="hidden" name="email" value="<%= email %>">
      <label for="otp" style="display:block; color: #ccc; margin-bottom: 8px;">Enter the OTP:</label>
      <input
        type="text"
        name="otp"
        placeholder="Enter 6-digit code"
        required
        pattern="\d{6}"
        maxlength="6"
        style="width: 100%; padding: 12px; border: 1px solid gold; background: black; color: white; border-radius: 5px;"
      />
      <button type="submit" class="btn" style="margin-top: 20px; background: gold; color: black; font-weight: bold; width: 100%;">
        Verify and Complete Registration
      </button>
    </form>
  </div>
</body>
</html>
